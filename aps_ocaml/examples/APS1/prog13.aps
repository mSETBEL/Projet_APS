[
  VAR x int;
  PROC REC incr [var n:int]
    [
      IF (lt x 42)
        [
	  SET x (add x n);
	  ECHO x;
	  CALL incr n
	]
	[ ECHO x ]
    ];
  SET x 10;
  ECHO x;

  VAR y int;
  SET y 2;
  CALL incr (adr y)
]